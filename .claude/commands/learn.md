# Learn

Quick-log a concept you just learned, without breaking flow.

## Instructions
- Identify the concept or term just discussed.
- Pick the right file in `docs/learning/` (create it if the topic is new), e.g.:
  - `docs/learning/nextjs.md`, `docs/learning/typescript.md`, `docs/learning/git.md`,
    `docs/learning/css-tailwind.md`, `docs/learning/deployment.md`, `docs/learning/web-basics.md`
- Add the concept with:
  - A simple one-line explanation (the user is ~1 month into real apps — keep it plain).
  - A concrete example from our project if possible.
  - Today's date.
- If the user said exactly what to log, log that. Otherwise log the most recent concept explained.
- Confirm what was logged and where (one line), then resume the previous task immediately.
