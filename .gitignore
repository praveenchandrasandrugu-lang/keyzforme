# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — NEVER commit secrets (document variable NAMES in README instead)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# brainstorming visual companion + playwright captures (scratch)
/.superpowers/
/.playwright-mcp/
/verify-*.jpeg

# NOTE: /mockups/ is intentionally committed — homepage.html is the approved
# design reference the real build must match.

# typescript
*.tsbuildinfo
next-env.d.ts

# personal convenience slash-commands that reference Praveen's private local
# Obsidian vault (W:\Keyz-Notes\) — local-only tooling, not for the public repo.
/.claude/commands/learn.md
/.claude/commands/vault-note.md
